[Output]
Format=directory
Output=katl-runtime-base-root
ImageId=katl-runtime-base

[Content]
Bootable=no
Packages=
        # Runtime boot, kernel modules, and initramfs tooling.
        kernel-core
        kernel-modules
        dracut
        systemd
        systemd-container
        systemd-udev
        dbus-broker
        # Host networking and service access.
        systemd-networkd
        systemd-resolved
        openssh-server
        ca-certificates
        util-linux
        iproute
        kmod
        nftables
        # Container host plumbing; Kubernetes packages live in the sysext.
        containerd
        crun
        containernetworking-plugins
        # Minimal shell and core userspace expected by systemd units and operators.
        coreutils
        bash
Hostname=katl-runtime

[Build]
WorkspaceDirectory=/tmp/katl-mkosi-runtime-base-workspace
