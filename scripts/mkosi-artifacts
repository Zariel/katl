#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"

if command -v katl-mkosi-artifacts >/dev/null 2>&1; then
    exec katl-mkosi-artifacts "$@"
fi

cd "$repo_root"
exec go run ./cmd/katl-mkosi-artifacts "$@"
