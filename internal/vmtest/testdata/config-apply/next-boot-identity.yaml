apiVersion: katl.dev/v1alpha1
kind: NodeConfigurationChange
metadata:
  sourceID: operator
  desiredVersion: "22"
apply:
  mode: next-boot
spec:
  clusterDefaults:
    identity:
      hostname: cp-1-staged
