apiVersion: katl.dev/v1alpha1
kind: NodeConfigurationChange
metadata:
  sourceID: operator
  desiredVersion: "20"
apply:
  mode: live
spec:
  clusterDefaults:
    networkd:
      files:
        - name: 20-vmtest-rejected.network
          content: |
            [Match]
            Name=*
            [Network]
            DHCP=yes
